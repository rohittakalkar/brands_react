/** Dev-facing switcher so the 5 BrandMcat variants can be compared side by side. Not part of
    any variant's actual design — hidden everywhere since it eats screen space a real buyer
    never needs. Variants stay reachable by direct URL (/v1 .. /v5). */
export default function BrandMcatVariantNav(props: { slug: string; category: string; active: number }) {
  void props;
  return null;
}
